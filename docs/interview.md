### Agent-f - SSE 스트리밍

SSE(Server-Sent Events) 프로토콜
EventSource vs Fetch(ReadableStream)
SSE vs WebSocket vs Long Polling
ReadableStream / TextDecoder
AbortController
Last-Event-ID
백프레셔·토큰 큐
연결 소유권을 컴포넌트가 아닌 전역 스토어(Zustand)에

### Agent-f - 렌더

시간 복잡도 O(N×M) → O(N+M), Map 인덱싱 / 복합 키
React 렌더링

### Challenger

Race condition / single-flight
in-flight Promise 공유
JWT access/refresh 토큰 구조, 토큰 rotation
토큰 저장 위치
axios 인터셉터
무한 루프 방지

### React/Next.js

Next.js App Router
TanStack Query(서버 상태) vs Zustand(클라 상태)
React 19 네이티브
FSD(Feature-Sliced Design)
디자인 시스템 / 성급한 추상화 / Rule of Three
